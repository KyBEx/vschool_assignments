// Given a string, return true if the string starts with 'hi'
// and false otherwise.

var startHi = function(str){
    if(str[0] === 'h' && str[1] === "i") {
        return true
    }
    return false
}
