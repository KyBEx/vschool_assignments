// this file will contain the root reducer
// will need to import reducer compiler from react-dom
import {combineReducers} from "react";
// have to import my reducers / components folder (make sure to make them feature based names)
export default combineReducers(
    {
        // takes an object of your reducers(will have to import)
    }
)
